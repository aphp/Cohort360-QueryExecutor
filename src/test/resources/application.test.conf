app {
    enableCache = false
    jobs {
        threads = 100
    }
    back {
        url = "http://django"
        authToken = "test"
    }
    cohortCreationLimit = 2000
}

spark {
    master = "local[*]"
    driver {
        port = 4000
        host = localhost
    }
}

postgres {
      host = localhost
      database = dummy
      schema = public
      user = test
      port = 5432
}

solr {
    zk = localhost
    max_try = 1
    rows = 10000
    commit_within = 10000
}
